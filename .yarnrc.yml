enableGlobalCache: true

nodeLinker: node-modules

# Third-party packages keep the default release-age quarantine; only this
# account's own scope is waived, so a just-published @libraz/* version can be
# pulled the moment it lands without lowering the bar for anything else.
npmPreapprovedPackages:
  - "@libraz/*"
